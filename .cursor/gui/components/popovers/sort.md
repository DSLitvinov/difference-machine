# Popover Sort

Figma: [Popover (Sort)](https://www.figma.com/design/qlwKiMPZblz96VSM2F3DlS/DFM-for-Cursor?node-id=4272-6727) (`4272:6727`).  
Код: `SortMenu`. 200×144.

Пункты сортировки сетки/списка — только из `get_design_context` на этот node. Не добавлять «sort by git status», если нет в макете. UI-only, не Forester.
